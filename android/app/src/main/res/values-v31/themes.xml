<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TransitKit" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash background: verde brand AppalRider. -->
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
        <!-- Icona splash centrata e dimensionata per stare dentro la safe zone
             della maschera circolare del sistema. Vedi drawable/splash_logo.xml. -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
    </style>
</resources>
